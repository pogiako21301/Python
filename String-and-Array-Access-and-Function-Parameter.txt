String/Array Access
[-1] -> returns the last character/element of a list.

[start:end] -> returns the element from index start up to index end.

FUNCTION PARAMETERS

def zxc (x):
with default values:
def zxc(x=10): -> Upon invocation, zxc () zxc (x=5)
def zxc(x=10,y=5,z): -> zxc (z=10)
			zxc(y=5,x=10,z=5)

String Manipulation

String -> List

to split a string with a seperator, we use split (seperator) - Method for String Objects